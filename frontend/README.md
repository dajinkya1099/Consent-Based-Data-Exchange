# Frontend

This is the Next.js frontend for the Consent Based Data Exchange Platform.

## Run locally

```powershell
cd frontend
npm install
npm run dev
```

## Notes

- Uses Next.js App Router, TypeScript, Tailwind CSS.
- Form progress is saved in `localStorage`.
- API calls are made to `http://127.0.0.1:8000/api` by default.
